# 체스 엔진 개발 계획

JavaScript + HTML로 체스판, 기물, 수 분석기, 엔진을 만드는 단계별 계획이다.

## 목표

브라우저에서 체스를 두고, 엔진이 합법수를 계산하고, 현재 수를 분석할 수 있는 체스 엔진을 만든다.

핵심 범위:

1. 체스판과 기물 표현
2. HTML/CSS 보드 UI
3. 체스 규칙(합법수, 체크, 캐슬링 등)
4. 수 분석기 (JavaScript)
5. 엔진 탐색 (미니맥스 + 알파베타)

## 기술 스택

| 구분 | 선택 | 이유 |
|------|------|------|
| 규칙/엔진/분석 | JavaScript | 브라우저에서 바로 실행 |
| UI | HTML + CSS | 보드를 화면에 그리기 |
| 실행 | 정적 파일 (`index.html`) | 서버 없이 열 수 있음 |
| 테스트 | 브라우저 콘솔 + 단위 테스트 파일 | 규칙 버그를 빨리 잡기 |

외부 체스 라이브러리는 쓰지 않는다. 규칙을 직접 구현하는 것이 엔진의 핵심이다.

## 권장 파일 구조

```
chesse_engine/
├── PLAN.md                 # 이 계획
├── README.md
├── index.html              # 보드 UI 진입점
├── css/
│   └── board.css           # 8x8 보드, 기물 스타일
└── js/
    ├── pieces.js           # 기물 종류, 유니코드/심볼
    ├── board.js            # 64칸 보드 상태
    ├── moves.js            # 기물별 이동, 합법수
    ├── rules.js            # 체크, 체크메이트, 캐슬링, 앙파상, 프로모션
    ├── analyzer.js         # 수 분석 (평가, 후보수)
    ├── engine.js           # 탐색 (미니맥스 / 알파베타)
    └── ui.js               # 클릭으로 두고 HTML 갱신
```

의존 방향:

```
pieces.js → board.js → moves.js → rules.js → analyzer.js → engine.js → ui.js
```

UI는 규칙/엔진만 호출한다. 엔진이 DOM을 직접 건드리지 않는다.

## 데이터 모델

### 기물

한 글자 표기 (FEN과 맞춤):

| 기물 | 백 | 흑 |
|------|----|----|
| 킹 | `K` | `k` |
| 퀸 | `Q` | `q` |
| 룩 | `R` | `r` |
| 비숍 | `B` | `b` |
| 나이트 | `N` | `n` |
| 폰 | `P` | `p` |

빈 칸은 `null` 또는 `"."`.

### 보드

- 길이 64 배열, 인덱스 `0` = a8, `63` = h1 (FEN 읽기 순서)
- 또는 8x8 2차원 배열 `[rank][file]`
- 좌표는 내부적으로 `{ file: 0~7, rank: 0~7 }` + 체스 표기 `e4` 변환 함수를 둔다

처음에는 **1차원 64칸 + 헬퍼**가 구현과 복사가 쉽다.

### 게임 상태 (`GameState`)

규칙이 보드 배열만으로는 부족하다. 상태에 함께 둔다.

- `board`: 64칸
- `sideToMove`: `'w' | 'b'`
- `castling`: `KQkq` 가능 여부
- `enPassant`: 앙파상 목표 칸 또는 `null`
- `halfmoveClock`: 50수 규칙
- `fullmoveNumber`: 흑이 둔 뒤 증가
- `history`: 되돌리기용 수 기록

FEN 문자열 ↔ `GameState` 변환을 초반에 만들면 테스트가 쉬워진다.

## 단계별 구현

### Phase 1 — 판과 기물 (UI 골격) ✅

목표: 시작 배치가 화면에 보인다. 아직 두면 안 된다.

1. `pieces.js`: 기물 상수, 색 판별, 유니코드 심볼 (♔♕♖♗♘♙ 등)
2. `board.js`: 빈 판 생성, 시작 배치, 칸 읽기/쓰기, 좌표 변환 (`e2` ↔ index)
3. `index.html` + `board.css` + `ui.js`: 8x8 칸 렌더, 기물 표시
4. 시작 FEN을 보드에 올려 확인

완료 기준:

- 흰 말은 아래(2랭크 폰, 1랭크 나머지), 검은 말은 위
- 밝은 칸/어두운 칸이 교차
- a1이 흰 룩

### Phase 2 — 클릭으로 두기 ✅

목표: 기물을 집어 다른 칸으로 옮긴다. 규칙은 아직 약하다.

1. 칸 클릭 → 선택 하이라이트
2. 두 번째 클릭 → 기물 이동
3. 자기 기물만 집기
4. 턴 교대 표시

구현할 때 Phase 3을 함께 끝냈다. 잘못된 수는 처음부터 막힌다.
선택한 기물의 갈 수 있는 칸을 점/테두리로 표시하고, 마지막 수와 체크받는 킹을 강조한다.
새 게임 / 무르기 / 보드 뒤집기 버튼도 함께 넣었다.

### Phase 3 — 합법수 생성 ✅

목표: 체스 규칙대로만 둔다.

기물별 의사수(pseudo-legal)부터 만들고, 자기 킹이 남는 수만 남긴다.

| 기물 | 이동 |
|------|------|
| 나이트 | L자 8방향, 장애물 없음 |
| 킹 | 인접 8칸 |
| 룩 | 가로/세로, 막히면 정지 |
| 비숍 | 대각선, 막히면 정지 |
| 퀸 | 룩 + 비숍 |
| 폰 | 전진 1, 시작 2칸, 대각 잡기, 앙파상, 프로모션 |

그다음 `rules.js`:

- 어떤 칸이 공격받는지
- 체크 / 체크메이트 / 스테일메이트
- 캐슬링 (킹·룩 미이동, 경로 비어 있음, 통과 칸 체크 아님)
- 앙파상
- 프로모션 (일단 퀸 자동, 이후 UI에서 선택)
- (선택) 3회 동형, 50수 규칙

완료 기준:

- 시작 위치에서 백 합법수 20개 ✅
- 자기 킹을 잡는 수 / 체크를 무시하는 수가 거부됨 ✅
- 간단한 퍼즐 몇 개에서 메이트를 인식 ✅

`npm test`로 확인한다. perft 5개 국면(시작 위치 깊이 4 = 197281, Kiwipete 깊이 3 = 97862 등)이
전부 맞고, 대국 시나리오 테스트가 캐슬링·앙파상·프로모션·무르기를 검증한다.

### Phase 4 — 수 분석기 (`analyzer.js`) ✅

엔진의 “눈”이다. 탐색 없이 현재 국면을 설명한다.

출력 예:

- 현재 턴, 체크 여부
- 합법수 목록 (대수 표기 `Nf3`, `exd5`, `O-O`)
- 캡처 / 체크 / 프로모션 수 표시
- 단순 평가 점수 (백 기준 centipawn)
- 기물 가치 합, (이후) 위치 보너스

기물 가치 (초기값):

```
P=100, N=320, B=330, R=500, Q=900, K=20000
```

UI에는 사이드 패널을 둔다.

- 합법수 리스트
- 평가 점수
- 마지막 수
- (이후) 엔진 추천 수

구현 결과:

- `evaluate(state)`는 기물 가치 합만 본다 (백 기준 centipawn). 위치 점수는 Phase 5에서 얹는다.
- `describeMove`가 SAN·UCI·캡처·체크·메이트·승격·캐슬링·즉시 이득(`gain`)을 붙인다.
  체크/메이트는 SAN이 이미 붙인 `+`/`#`를 읽어 쓴다 — 탐색을 두 번 하지 않는다.
- `analyze(state)`는 후보수를 메이트 → 이득 → 체크 → 표기 순으로 정렬해 돌려준다.
- 패널에 평가 막대(±10폰), 캡처/체크/승격 개수, 후보수 버튼(클릭하면 그 수를 둔다)을 넣었다.
- `tests/analyzer.js`가 평가 대칭성, 이득 계산, 분류, 정렬을 검증한다.

### Phase 5 — 엔진 (`engine.js`) ✅

목표: 사람이 둔 뒤 엔진이 응수한다.

1. 평가 함수: 기물 점수 + 간단한 위치 점수 (중앙 통제, 폰 구조는 나중에)
2. 미니맥스 (깊이 2~3)
3. 알파베타 가지치기
4. 캡처 우선 정렬로 가지치기 효율 올리기
5. 깊이/시간 제한 (예: 깊이 3, 또는 1초)

완료 기준:

- 오프너에서 말이 앞으로 나오고, 공짜 기물을 잡음 ✅
- 1수 메이트를 놓치지 않음 (깊이 충분할 때) ✅

구현 결과:

- 네가맥스 + 알파베타, 반복 심화(깊이 1→N), 시간 제한. 끊긴 깊이의 결과는 버린다.
- 평가는 `analyzer.evaluate`(재료) + `positionScore`(piece-square table). 재료 평가는
  analyzer에 그대로 두고, 위치 점수는 engine이 얹는다 — 분석 패널과 엔진이 서로를 흔들지 않는다.
- 엔드게임에서 킹 표를 바꾸고, 재료가 룩 이상 앞서면 상대 킹을 구석으로 모는 항을 더한다.
  이게 없으면 K+Q vs K에서 퀸만 돌다 50수 규칙에 걸린다.
- 캡처 우선 정렬(MVV-LVA) + 정지 탐색(캡처만, 체크 중에는 전체 수). 정지 탐색이 없으면
  깊이 끝에서 "방금 잡았다"는 국면을 좋게 보고 기물을 그냥 준다.
- UI: 엔진이 흑 토글, 깊이 선택(2~5), 추천 수 버튼(보드에 파란 테두리로 표시).
  탐색은 동기라 "생각 중…"을 먼저 그리고 한 틱 뒤에 돌린다.
- `tests/engine.js`가 가지치기 정확성(전폭 탐색과 점수 대조), 1수 메이트, 공짜 기물,
  지켜진 기물 회피, 시간 제한, K+Q vs K 외통까지 확인한다. 시작 위치 깊이 3은 1,878노드 30ms.

### Phase 6 — 게임 흐름 정리

- 새 게임, 무르기
- FEN 불러오기 / 내보내기
- 수 기록 (PGN 간단 버전)
- 프로모션 선택 UI
- 게임 종료 메시지 (메이트, 스테일, 기권)

## 수 분석기와 엔진의 역할 분리

| 모듈 | 하는 일 | 하지 않는 일 |
|------|---------|--------------|
| `moves.js` / `rules.js` | 합법수, 규칙 | 점수, 추천 |
| `analyzer.js` | 현재 수 설명, 정적 평가 | 깊은 탐색 |
| `engine.js` | 재귀 탐색으로 최선수 | DOM |
| `ui.js` | 클릭, 그리기, 분석 결과 표시 | 규칙 계산 |

분석기는 엔진이 쓰는 평가 함수를 재사용한다. 탐색은 엔진만 한다.

## 화면 구성 (HTML)

```
+------------------+------------------+
|                  |  턴 / 평가       |
|     8x8 보드     |  합법수 목록     |
|                  |  엔진 추천       |
|                  |  수 기록         |
+------------------+------------------+
| [새 게임] [무르기] [엔진 on/off]    |
```

기물은 처음엔 유니코드로 충분하다. 이미지 스프라이트는 보드가 안정된 뒤 교체한다.

## 테스트 전략

규칙 버그가 엔진을 망친다. 탐색보다 규칙 테스트를 먼저 둔다.

1. `board.js`: FEN 라운드트립
2. 시작 위치 합법수 20 / 2수 후 400 (perft 깊이 2)
3. 알려진 FEN에서 perft 값 비교
4. 분석기: 기물 점수 합이 기대값과 같은지
5. 엔진: K+Q vs K 같은 쉬운 메이트를 찾는지

`js/perft.js` 또는 `tests/`에 작은 테스트 페이지를 둔다.

## 하지 않을 것 (1차)

범위를 고정한다.

- 오프닝 북, 엔드게임 테이블베이스
- 웹 워커 병렬 탐색 (필요해지면 Phase 5 이후)
- 온라인 대국 / 계정
- NNUE / 신경망 평가
- TypeScript 전환 (동작이 안정된 뒤 검토)

## 작업 순서 (바로 다음)

Phase 1~5 완료. 엔진과 한 판 둘 수 있다.

마지막은 Phase 6 — 게임 흐름 정리:

1. FEN 붙여넣기로 국면 불러오기 / 현재 FEN 복사
2. PGN 내보내기 (기보는 이미 SAN으로 쌓여 있다)
3. 기권, 게임 종료 메시지 정리
4. (선택) 엔진 색 고르기, 엔진 대 엔진

규칙을 고칠 때마다 `npm test`를 먼저 돌린다. perft가 깨지면 엔진도 같이 틀린다.
